# unite-roulette 🎰

ポケモンユナイトの **チーム編成をランダム抽選** するWebアプリＵ。
人数を選んで抽選すると、ポケモンと技構成（わざ1・わざ2）がランダムに決まります。

- **状態**: 🛠 MVP（動作する最小版）
- **種別**: Web（React + TypeScript + Vite）

## できること

- **人数を1〜5人で選択**して一括抽選（チーム内でポケモンは重複しない）
- **プレイヤー名を入力**でき、結果に表示（ブラウザに保存される）
- **枠ごとにロール指定**（おまかせ／アタック／ディフェンス／バランス／スピード／サポート）
- 抽選時は **名前がパラパラ切り替わって1人ずつ確定する演出**
- ゲーム仕様に対応した抽選: **技固定**（1択）、**組み合わせ固定**（ウーラオス）、**試合中変更可**（バシャーモ・ミュウは「自由」表示）
- **編集画面**でポケモン・技を追加／修正／削除（コードを触らず更新できる）
  - 変更は**ブラウザに自動保存**され、リロードしても残る
  - **JSON書き出し／読み込み**でバックアップ・共有・アプデ反映
  - **初期データに戻す**でリセット

## 使い方（開発）

```bash
npm install     # 初回のみ：依存をインストール
npm run dev     # 開発サーバー起動（http://localhost:5173）
npm run build   # 本番用にビルド（dist/ が生成される）
npm run preview # ビルド結果をローカルで確認
```

型チェック（TypeScript）:

```bash
npx tsc -b
```

## 構成

```
src/
├─ data/
│  ├─ types.ts     … データの「型」定義（Pokemon / Role など）
│  └─ pokemon.ts   … 初期ポケモンデータ（SEED_POKEMON）
├─ lib/
│  ├─ draw.ts      … 抽選ロジック（純粋関数・画面から独立）
│  └─ storage.ts   … ブラウザ保存・JSON入出力・壊れたデータ対策
├─ components/
│  ├─ Roulette.tsx … 抽選画面＋演出
│  └─ Editor.tsx   … 編集画面
└─ App.tsx         … データを1か所で持ち、タブで画面を出し分け
```

## ロードマップ

- [x] 現行の全ポケモン（94体）をデータに追加
- [ ] Webに公開（デプロイ）
- [ ] 抽選結果の履歴・シェア

## データについて

- ポケモン95体分（メガシンカ・フォルム違い・ストライク/ハッサム個別を含む）のロール・技を収録。
- 出典: [ポケモンユナイト攻略Wiki（wikiwiki.jp/poke-unite）](https://wikiwiki.jp/poke-unite/) ＋ 実プレイ知識による補正。
- **技名は最新アプデと差異がある場合があります**。編集画面からいつでも修正できます。
- データは `src/data/pokemon.json` に分離。更新したら `pokemon.ts` の `DATA_VERSION` を+1すると、
  公開サイトの訪問者のブラウザ保存データも自動で新しいものに置き換わります
  （訪問者の編集内容はリセットされるので注意）。
